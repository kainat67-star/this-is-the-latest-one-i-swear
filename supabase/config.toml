# Minimal Supabase CLI config. Link your project: `supabase link --project-ref <ref>`
# Deploy this function: `supabase functions deploy google-ads-auth`
# Secrets: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, APP_URL (optional if client sends redirect_uri)

[functions.google-ads-auth]
verify_jwt = false
